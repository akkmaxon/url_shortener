<% unless @result.nil? %>

  <h1>Found <%= @result.count %> '<%= params[:search_keywords] %>' in '<%= params[:commit] %>':</h1>

  <%= will_paginate @result %>

  <%= render 'layouts/list_urls', urls: @result %>

  <%= will_paginate @result %>

<% end %>
