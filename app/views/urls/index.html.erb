<h1>All your urls</h1>

<ul>
<% @urls.each do |url| %>
  <li>
    <%= url.original %> (<%= show_full_address(url.short) %>)
    <%= link_to 'Edit', edit_url_path(url) %> | 
    <%= link_to 'Delete', url, method: :delete, data: { confirm: 'Are you sure?' } %>
  </li>
<% end %>
</ul>
